# メール送信設定（SMTP）
# お問い合わせフォームから送信されたメールを order@yamato-chemi.co.jp と info@yamato-chemi.co.jp に送信します

# SMTPサーバーのホスト名
SMTP_HOST=smtp.example.com

# SMTPサーバーのポート番号（通常は587または465）
SMTP_PORT=587

# SSL/TLS接続を使用するか（465の場合はtrue、587の場合はfalse）
SMTP_SECURE=false

# SMTPサーバーの認証情報
SMTP_USER=your-email@example.com
SMTP_PASS=your-password

# 送信元メールアドレス（通常はSMTP_USERと同じ）
SMTP_FROM=your-email@example.com
